language: node_js
node_js:
  - lts/*
services:
  - postgresql

before_script:
  - psql -c 'CREATE DATABASE travis_ci_test;' -U postgres
  - psql -c "CREATE USER travis WITH PASSWORD '123';" -U postgres
